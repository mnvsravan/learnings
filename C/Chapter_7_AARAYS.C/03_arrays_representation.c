#include <stdio.h>

int main() {
    int marks[] = {9, 8, 8};   // array initialization

    for (int i = 0; i < 3; i++) {
        printf("The value of marks[%d] is %d\n", i, marks[i]);
    }

    return 0;
}
// other representation of arrays 