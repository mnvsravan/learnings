#include <stdio.h>

int main() {
    int marks [90];
    marks [0]=1;
    marks [1]=23;
    printf("The value of marks 0 and marks 1 is %d and %d", marks[0], marks[1]);
    return 0;

} // We can keep writing this like till 90 marks 