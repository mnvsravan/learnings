#include <stdio.h>

int main(){
    int a, b, c ,d; // Declare multiple integer variables
    a = 5; // Initialize variable a with a value
    b = 10;             // Initialize variable b with a value  
    c = 6;
    d = 15; // Initialize variable d with a value
    int i, j;
    i=j= 20; // Initialize multiple variables i and j with the same value
   
    printf("The value of compound is i+j-b+c-d: %d\n", i + j - b + c - d); // Print the result of a compound expression

    return 0;
}