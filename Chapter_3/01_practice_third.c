#include <stdio.h>

int main() {
    int a=10;
    if (a==11){
        printf("a is equal to 11\n");
    } else {
        printf("a is not equal to 11\n");
    }
    return 0;
}
// we need to use '==' for comparison, not '=' which is an assignment operator
    
    

